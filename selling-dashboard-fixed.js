const SUPABASE_URL='https://twfbmjwwqzxdxvclxbun.supabase.co';
const KEY_STORAGE='tradeflow_subscriber_publishable_key',SESSION_STORAGE='tradeflow_subscriber_session';
let key=null,session=null,tenantId=null,editingListingId=null,$=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]||c));
function msg(t,type=''){$('message').className=`small ${type}`.trim();$('message').textContent=t||''}function money(v,c='GBP'){if(v==null)return'—';try{return new Intl.NumberFormat('en-GB',{style:'currency',currency:c}).format(Number(v))}catch{return`${c} ${v}`}}
async function api(path,options={}){if(!key)throw Error('TradeFlow test-lab publishable key is not connected.');const h=new Headers(options.headers||{});h.set('apikey',key);h.set('Content-Type','application/json');if(session?.access_token)h.set('Authorization',`Bearer ${session.access_token}`);const r=await fetch(`${SUPABASE_URL}${path}`,{...options,headers:h});const text=await r.text();let b=null;try{b=text?JSON.parse(text):null}catch{b=text}if(!r.ok)throw Error(b?.message||b?.msg||b?.error||text||`HTTP ${r.status}`);return b}
let rows=[],soldRows=[],assets=[],channels=[],categories=[],branches=[],sourceMedia=[],sourceInspectionMedia=[],latestInspection=null;
async function waitForSubscriber(){
 const ready=window.tradeflowSubscriberAuthReady;
 const immediate=window.tradeflowSubscriberAuth;
 if(immediate?.session?.access_token){
   key=immediate.key;session=immediate.session;tenantId=immediate.tenantId||new URLSearchParams(location.search).get('tenant_id')||localStorage.getItem('tradeflow_subscriber_tenant_id');
   if(tenantId)return;
 }
 if(!ready)throw Error('Subscriber authentication layer did not load.');
 const auth=await Promise.race([
   ready,
   new Promise((_,reject)=>setTimeout(()=>reject(Error('Subscriber authentication did not finish loading. Please refresh the page or sign in again.')),8000))
 ]);
 if(!auth?.session?.access_token)throw Error('Subscriber authentication did not provide an access token.');
 key=auth.key;session=auth.session;tenantId=auth.tenantId;
}
async function load(){try{await waitForSubscriber();if(!tenantId)throw Error('This selling workspace requires a valid subscriber tenant.');msg('Loading…');const focusedAsset=new URLSearchParams(location.search).get('asset');if(focusedAsset){const q=`/rest/v1/listings?select=id,asset_id,channel_id,category_id,branch_id,listing_reference,status,title,description,asking_price,currency,quantity,listing_data,published_at,reserved_at,sold_at,delisted_at,created_at,updated_at&tenant_id=eq.${encodeURIComponent(tenantId)}&asset_id=eq.${encodeURIComponent(focusedAsset)}&status=not.in.(sold,delisted)&order=updated_at.desc`;rows=await api(q)||[];$('listings-panel').hidden=true;$('listings-panel').style.display='none';$('listings').innerHTML='';}else{const filter=$('status-filter').value;let q=`/rest/v1/listings?select=id,asset_id,channel_id,category_id,branch_id,listing_reference,status,title,description,asking_price,currency,quantity,listing_data,published_at,reserved_at,sold_at,delisted_at,created_at,updated_at&tenant_id=eq.${encodeURIComponent(tenantId)}&status=not.in.(sold,delisted)&order=updated_at.desc`;if(filter&&filter!=='sold'&&filter!=='delisted')q+=`&status=eq.${encodeURIComponent(filter)}`;rows=await api(q)||[];render();}await loadLookups();await loadSoldListings();if(focusedAsset)await renderChannelMatrix(focusedAsset);applyFocusedProductMode();if(focusedAsset&&rows[0])await beginEdit(rows[0].id,true);msg(focusedAsset?(rows[0]?'Existing product listing loaded for editing.':'Product workspace loaded.'):`${rows.length} listing(s) loaded.`,'success')}catch(e){msg(e.message||String(e),'error')}}
async function loadSoldListings(){try{const data=await rpc('subscriber_get_sold_retail_items',{p_tenant_id:tenantId})||[];soldRows=data.map(x=>({...x,item:{title:x.title}}));renderSoldListings();}catch(e){soldRows=[];const el=$('sold-listings');if(el)el.innerHTML='<div class="small error">Sold items could not be loaded: '+esc(e.message||String(e))+'</div>';}}

function renderSoldListings(){const el=$('sold-listings');if(!el)return;if(!soldRows.length){el.innerHTML='<div class="empty">No paid retail items are waiting for dispatch.</div>';return}el.innerHTML='<div class="listing-table">'+soldRows.map(x=>{const f=x.fulfilment,s=f?.status||'awaiting';const tracking=f?.tracking_number?(f.tracking_url?'<a href="'+esc(f.tracking_url)+'" target="_blank" rel="noopener">'+esc(f.tracking_number)+'</a>':esc(f.tracking_number)):'';let action='';if(!f||s==='awaiting')action='<a class="listing-action primary" href="fulfilment-dashboard.html?order_id='+encodeURIComponent(x.order.id)+'">CREATE SHIPPING LABEL</a>';else if(s==='label')action='<button class="listing-action primary" type="button" data-dispatch="'+esc(f.id)+'">MARK AS SENT</button>';else action='<span class="listing-status">'+esc(s==='dispatched'?'Shipped':s==='delivered'?'Delivered':'Returned')+'</span>';return '<div class="listing-row sold"><div class="listing-reference-cell"><span class="listing-cell-label">Listing</span><strong>'+esc(x.listing_reference)+'</strong></div><div class="listing-title-cell"><span class="listing-cell-label">Product</span><strong>'+esc(x.title||x.item?.title||'Sold item')+'</strong><div class="small">Order '+esc(x.order.order_reference)+'</div></div><div><span class="listing-cell-label">Customer</span><span>'+esc(x.order.customer_name||'Customer')+'</span></div><div><span class="listing-cell-label">Paid</span><strong>'+money(x.order.total,x.order.currency)+'</strong></div><div><span class="listing-cell-label">Shipping</span><span>'+esc(s==='dispatched'?'Shipped':s==='delivered'?'Delivered':s==='label'?'Label ready':'Awaiting label')+(tracking?'<br>'+tracking:'')+'</span></div><div><span class="listing-cell-label">Action</span><div class="listing-actions">'+action+'</div></div></div>'}).join('')+'</div>';document.querySelectorAll('[data-dispatch]').forEach(b=>b.onclick=()=>markSoldDispatched(b.dataset.dispatch,b));}
async function markSoldDispatched(id,b){if(!id)return;b.disabled=true;try{const row=soldRows.find(x=>x.fulfilment?.id===id);const f=row?.fulfilment;if(!f)throw Error('Fulfilment record not found.');if(f.status!=='label')throw Error('Shipping label must be ready before marking the item as sent.');msg('Marking shipment as sent…');const payload={p_tenant_id:tenantId,p_entity_type:'fulfilment',p_entity_id:id,p_expected_from:'label',p_to_status:'dispatched',p_notes:null,p_metadata:{source:'selling-dashboard'}};await api('/rest/v1/rpc/transition_workflow_entity',{method:'POST',body:JSON.stringify(payload)});if(row?.order?.status==='paid'){const orderPayload={p_tenant_id:tenantId,p_entity_type:'retail_order',p_entity_id:row.order.id,p_expected_from:'paid',p_to_status:'fulfilment',p_notes:'Shipment dispatched.',p_metadata:{source:'selling-dashboard'}};await api('/rest/v1/rpc/transition_workflow_entity',{method:'POST',body:JSON.stringify(orderPayload)});}await loadSoldListings();msg('Item marked as sent. The customer order will now show Shipped and the tracking details.','success')}catch(e){msg(e.message||String(e),'error')}finally{b.disabled=false}}

async function renderChannelMatrix(assetId){const el=$('channel-matrix');if(!el)return;try{const allChannels=channels;const listings=await api(`/rest/v1/listings?select=id,channel_id,status,listing_reference,asking_price,currency,title&tenant_id=eq.${encodeURIComponent(tenantId)}&asset_id=eq.${encodeURIComponent(assetId)}&order=updated_at.desc`)||[];if(!allChannels.length){el.innerHTML='<div class="empty">No sales channels are configured.</div>';return}el.innerHTML=allChannels.map(c=>{const l=listings.find(x=>x.channel_id===c.id&& !['sold','delisted'].includes(String(x.status||'').toLowerCase()));const status=l?statusLabel(l.status):'Not listed';const connected=!!l;return `<div class="channel-matrix-row ${connected?'connected':'not-listed'}"><div><strong>${esc(String(c.channel_type||'').toLowerCase()==='storefront'?'Website':c.name)}</strong><div class="small">${l?esc(l.listing_reference):'No listing for this product'}</div></div><div class="channel-matrix-status">${esc(status)}</div><div>${l?money(l.asking_price,l.currency):'—'}</div><div>${l?`<button type="button" class="channel-matrix-action" data-channel-edit="${l.id}">EDIT LISTING</button>`:'Not listed'}</div></div>`}).join('');document.querySelectorAll('[data-channel-edit]').forEach(b=>b.onclick=()=>beginEdit(b.dataset.channelEdit,true))}catch(e){el.innerHTML=`<div class="small error">Channel status could not be loaded: ${esc(e.message||String(e))}</div>`}}

async function loadLookups(){
 const focusAsset=new URLSearchParams(location.search).get('asset');
 const assetQuery=focusAsset
   ? `/rest/v1/inventory_assets?select=id,asset_reference,title,status,description,condition_grade,serial_number,quantity,purchase_price,current_value,currency,location,category_id,branch_id,buying_item_id&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(focusAsset)}&limit=1`
   : `/rest/v1/inventory_assets?select=id,asset_reference,title,status,description,condition_grade,serial_number,quantity,purchase_price,current_value,currency,location,category_id,branch_id,buying_item_id&tenant_id=eq.${encodeURIComponent(tenantId)}&status=in.(ready_for_sale,listed)&order=asset_reference`;
 const results=await Promise.allSettled([
   api(assetQuery),
   api(`/rest/v1/sales_channels?select=id,name,channel_type,slug,active&tenant_id=eq.${encodeURIComponent(tenantId)}&active=eq.true&order=name`),
   api(`/rest/v1/categories?select=id,name&tenant_id=eq.${encodeURIComponent(tenantId)}&active=eq.true&selling_enabled=eq.true&order=sort_order,name`),
   api(`/rest/v1/category_branches?select=id,category_id,name&tenant_id=eq.${encodeURIComponent(tenantId)}&active=eq.true&selling_enabled=eq.true&order=sort_order,name`)
 ]);
 assets=results[0].status==='fulfilled'?(results[0].value||[]):[];
 channels=results[1].status==='fulfilled'?(results[1].value||[]):[];
 categories=results[2].status==='fulfilled'?(results[2].value||[]):[];
 branches=results[3].status==='fulfilled'?(results[3].value||[]):[];
 $('asset').innerHTML='<option value="">Select inventory asset…</option>'+assets.map(x=>`<option value="${x.id}">${esc(x.asset_reference)} — ${esc(x.title||'Untitled')}</option>`).join('');
 $('channel').innerHTML='<option value="">Select sales channel…</option>'+channels.map(x=>{const label=String(x.channel_type||'').toLowerCase()==='storefront'?'Website':(x.name||x.channel_type||'Sales channel');return `<option value="${x.id}">${esc(label)}</option>`}).join('');
 if(focusAsset&&assets.some(x=>x.id===focusAsset))$('asset').value=focusAsset;
 $('asset').onchange=()=>syncAssetContext();
 syncAssetContext();
 const failed=results.filter(x=>x.status==='rejected');
 if(failed.length)console.warn('Some Selling lookups failed:',failed.map(x=>x.reason?.message||String(x.reason)));
}

async function signMedia(media){if(!media?.storage_path)return null;try{const r=await fetch(`${SUPABASE_URL}/storage/v1/object/sign/tradeflow-media/${media.storage_path}`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${session?.access_token||''}`,'Content-Type':'application/json'},body:JSON.stringify({expiresIn:3600})});const b=await r.json();return b?.signedURL?`${SUPABASE_URL}/storage/v1${b.signedURL}`:null}catch{return null}}

async function getMediaForItem(itemId,assetId){const buyingLinks=await api(`/rest/v1/buying_item_media?select=media_asset_id,sort_order&tenant_id=eq.${encodeURIComponent(tenantId)}&buying_item_id=eq.${encodeURIComponent(itemId)}&order=sort_order`)||[];const inventoryLinks=assetId?await api(`/rest/v1/inventory_asset_media?select=media_asset_id,sort_order&tenant_id=eq.${encodeURIComponent(tenantId)}&inventory_asset_id=eq.${encodeURIComponent(assetId)}&order=sort_order`)||[]:[];const links=[...buyingLinks,...inventoryLinks].filter((x,i,a)=>a.findIndex(y=>y.media_asset_id===x.media_asset_id)===i);const out=[];for(const link of links){const m=(await api(`/rest/v1/media_assets?select=id,storage_path,original_filename,mime_type,byte_size,asset_kind&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(link.media_asset_id)}` )||[])[0];if(m){m.signedUrl=await signMedia(m);m.sort_order=link.sort_order||0;out.push(m)}}return out}

async function loadSourceInformation(asset){if(!asset?.buying_item_id){$('source-information').innerHTML='<div class="empty">This inventory asset has no linked buying item.</div>';$('sales-photos').innerHTML='<div class="empty">No source photographs are linked to this inventory item.</div>';return}try{const items=await api(`/rest/v1/buying_items?select=id,buying_request_id,title,description,quantity,item_condition,category_id,branch_id,created_at&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(asset.buying_item_id)}`);const item=items?.[0];if(!item)throw Error('The original buying item could not be loaded.');const requests=await api(`/rest/v1/buying_requests?select=id,request_reference,status,source,notes,customer_id,submitted_at&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(item.buying_request_id)}`);const request=requests?.[0];let customerDetails=null;try{customerDetails=await api('/rest/v1/rpc/subscriber_get_buying_item_customer_details',{method:'POST',body:JSON.stringify({p_tenant_id:tenantId,p_buying_item_id:item.id})});}catch{}const customerFields=Array.isArray(customerDetails?.fields)?customerDetails.fields:[];let inspections=[];try{inspections=await api(`/rest/v1/buying_item_inspections?select=id,outcome,condition_grade,notes,passed,inspected_at,metadata&tenant_id=eq.${encodeURIComponent(tenantId)}&buying_item_id=eq.${encodeURIComponent(item.id)}&order=inspected_at.desc`)||[]}catch{}latestInspection=inspections[0]||null;const inspection=latestInspection;const allMedia=await getMediaForItem(item.id,asset.id);sourceMedia=allMedia.filter(m=>m.asset_kind!=='inspection_photo');sourceInspectionMedia=allMedia.filter(m=>m.asset_kind==='inspection_photo');const cat=categories.find(x=>x.id===asset.category_id);const branch=branches.find(x=>x.id===asset.branch_id);$('category-display').textContent=cat?.name||'Inherited from original buying category';$('branch-display').textContent=branch?.name||'Inherited from original buying branch';const fields=customerFields.length?customerFields.map(f=>`<div><span class="field-label">${esc(f.label||f.field_key)}</span><strong>${esc(typeof f.value==='object'&&f.value!==null?JSON.stringify(f.value):f.value??'—')}</strong></div>`).join(''):'<div class="small">No additional structured customer fields were supplied.</div>';const checks=inspection?.metadata?.checks||{};$('source-information').innerHTML=`<div class="source-grid"><div class="source-card"><h3>Customer submission</h3><p><strong>Customer:</strong> ${esc([customerDetails?.customer?.first_name,customerDetails?.customer?.last_name].filter(Boolean).join(' ')||'Not provided')} · ${esc(customerDetails?.customer?.email||'Not provided')}</p><p><strong>Customer reference:</strong> ${esc(customerDetails?.customer?.customer_reference||'Not provided')}</p><p><strong>Request:</strong> ${esc(request?.request_reference||'—')}</p><p><strong>Item:</strong> ${esc(item.title||asset.title||'—')}</p><p><strong>Customer description</strong></p><div>${esc(item.description||'No description supplied.')}</div><p><strong>Customer condition</strong></p><div>${esc(item.item_condition||'Not supplied')}</div><p><strong>Request notes / customer explanation</strong></p><div>${esc(customerDetails?.request_notes||request?.notes||'Not provided')}</div><div style="margin-top:12px" class="inspection-checks">${fields}</div></div><div class="source-card"><h3>Inspection</h3>${inspection?`<p><strong>Outcome:</strong> ${esc(inspection.outcome||'—')}</p><p><strong>Inspection condition:</strong> ${esc(['poor','good','very good','excellent','opened','never used','sealed'].includes(String(inspection.condition_grade||'').trim().toLowerCase())?inspection.condition_grade:'Legacy inspection grade recorded — select the new retail condition below.')}</p><p><strong>Inspection notes:</strong></p><div>${esc(inspection.notes||'No inspection notes supplied.')}</div><div style="margin-top:12px" class="inspection-checks"><div><span class="field-label">Package / accessories</span><strong>${esc(checks.package_accessories||'—')}</strong></div><div><span class="field-label">Serial / model</span><strong>${esc(checks.serial_verification||'—')}</strong></div><div><span class="field-label">Physical condition</span><strong>${esc(checks.physical_condition||'—')}</strong></div><div><span class="field-label">Function / technical test</span><strong>${esc(checks.functional_test||'—')}</strong></div><div><span class="field-label">Discrepancies</span><strong>${esc(inspection.metadata?.discrepancies||'None')}</strong></div></div>`: '<div class="small">No completed inspection record was found.</div>'}</div></div>`;renderSourcePhotos();applyInspectionToListingForm(item,inspection)}catch(e){$('source-information').innerHTML=`<div class="small error">Source information could not be loaded: ${esc(e.message||String(e))}</div>`;$('sales-photos').innerHTML='<div class="empty">Photographs could not be loaded.</div>'}}

function applyInspectionToListingForm(item,inspection){const base=String(item?.description||'').trim();const notes=String(inspection?.notes||'').trim();const discrepancies=String(inspection?.metadata?.discrepancies||'').trim();const parts=[];if(base)parts.push(base);if(notes)parts.push('Inspection notes: '+notes);if(discrepancies&& !/^none$/i.test(discrepancies))parts.push('Inspection discrepancies: '+discrepancies);if($('description'))$('description').value=parts.join('\n\n');if($('asset-condition')){const raw=String(inspection?.condition_grade||'').trim().toLowerCase();const valid=['poor','good','very good','excellent','opened','never used','sealed','very_good','never_used'];$('asset-condition').value=valid.includes(raw)?raw.replaceAll(' ','_'):'';$('asset-condition').dataset.sourceCondition=raw||'';}if($('condition-source-note'))$('condition-source-note').textContent=inspection?.condition_grade?'Inspection recorded condition: '+inspection.condition_grade+'. Select the new retail condition wording if the recorded value uses the old A–D scale.':'Condition has not been recorded in the inspection.';}

function renderSourcePhotos(){const all=[...sourceMedia,...sourceInspectionMedia];if(!all.length){$('sales-photos').innerHTML='<div class="empty">No customer or inspection photographs are attached to this item yet.</div>';return}$('sales-photos').innerHTML=`<div class="source-photo-grid">${all.map(m=>`<figure><img src="${m.signedUrl||''}" alt="${esc(m.original_filename)}"><figcaption>${m.asset_kind==='inspection_photo'?'Inspection photograph':'Customer photograph'} · ${esc(m.original_filename)}</figcaption></figure>`).join('')}</div>`}

async function loadListingPhotos(listingId){if(!listingId){$('listing-photos').innerHTML='<div class="empty">Save the listing before adding retail listing photographs.</div>';return}try{const links=await api(`/rest/v1/listing_media?select=id,media_asset_id,sort_order&tenant_id=eq.${encodeURIComponent(tenantId)}&listing_id=eq.${encodeURIComponent(listingId)}&order=sort_order`)||[];const photos=[];for(const link of links){const m=(await api(`/rest/v1/media_assets?select=id,storage_path,original_filename,mime_type,byte_size,asset_kind&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(link.media_asset_id)}`)||[])[0];if(m){m.signedUrl=await signMedia(m);m.linkId=link.id;m.sort_order=link.sort_order||0;photos.push(m)}}if(!photos.length){$('listing-photos').innerHTML='<div class="empty">No retail listing photographs have been added yet.</div>';return}$('listing-photos').innerHTML=`<div class="source-photo-grid listing-photo-grid">${photos.map(m=>`<figure><img src="${m.signedUrl||''}" alt="${esc(m.original_filename)}"><figcaption>Retail listing photograph · ${esc(m.original_filename)}<button type="button" class="listing-photo-remove" data-listing-media="${m.linkId}">REMOVE FROM LISTING</button></figcaption></figure>`).join('')}</div>`;document.querySelectorAll('[data-listing-media]').forEach(b=>b.onclick=()=>removeListingPhoto(b.dataset.listingMedia,listingId))}catch(e){$('listing-photos').innerHTML=`<div class="small error">Listing photographs could not be loaded: ${esc(e.message||String(e))}</div>`}}

async function uploadListingPhotos(){if(!editingListingId)return msg('Open an existing listing with EDIT before adding retail listing photographs.','error');const files=[...($('listing-photo-upload')?.files||[])];if(!files.length)return msg('Select one or more listing photographs.','error');const button=$('upload-listing-photos');button.disabled=true;try{let existing=await api(`/rest/v1/listing_media?select=sort_order&tenant_id=eq.${encodeURIComponent(tenantId)}&listing_id=eq.${encodeURIComponent(editingListingId)}&order=sort_order`)||[];let order=existing.length?Math.max(...existing.map(x=>Number(x.sort_order)||0))+1:0;for(const file of files){if(!file.type.startsWith('image/'))throw Error(file.name+' is not an image.');if(file.size>10*1024*1024)throw Error(file.name+' is larger than 10MB.');const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=`${tenantId}/listings/${editingListingId}/${crypto.randomUUID()}.${ext}`;const h=new Headers({'Content-Type':file.type,'x-upsert':'false',apikey:key});if(session?.access_token)h.set('Authorization',`Bearer ${session.access_token}`);const rr=await fetch(`${SUPABASE_URL}/storage/v1/object/tradeflow-media/${path}`,{method:'POST',headers:h,body:file});if(!rr.ok)throw Error(await rr.text()||'Listing photograph upload failed.');const media=(await api('/rest/v1/media_assets',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({tenant_id:tenantId,storage_bucket:'tradeflow-media',storage_path:path,original_filename:file.name,mime_type:file.type,byte_size:file.size,asset_kind:'listing_photo',retention_policy:'sold_90_days_after_sale',created_by:session?.user?.id||null})}))[0];if(!media?.id)throw Error('The listing photograph record could not be created.');await api('/rest/v1/listing_media',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({tenant_id:tenantId,listing_id:editingListingId,media_asset_id:media.id,sort_order:order++})})}await loadListingPhotos(editingListingId);$('listing-photo-upload').value='';msg('Retail listing photographs updated.','success')}catch(e){msg(e.message||String(e),'error')}finally{button.disabled=false}}

async function removeListingPhoto(linkId,listingId){if(!linkId||!listingId)return;if(!confirm('Remove this photograph from the listing? The original media file will remain in TradeFlow.'))return;try{await api(`/rest/v1/listing_media?id=eq.${encodeURIComponent(linkId)}&tenant_id=eq.${encodeURIComponent(tenantId)}`,{method:'DELETE'});await loadListingPhotos(listingId);msg('Photograph removed from the listing.','success')}catch(e){msg(e.message||String(e),'error')}}

async function uploadSalesPhotos(){const asset=assets.find(x=>x.id===$('asset').value);const files=[...($('sales-photo-upload')?.files||[])];if(!asset)return msg('Select an inventory asset first.','error');if(!files.length)return msg('Select one or more photographs.','error');const button=$('upload-sales-photos');button.disabled=true;try{let order=sourceMedia.length+sourceInspectionMedia.length;for(const file of files){if(!file.type.startsWith('image/'))throw Error(file.name+' is not an image.');if(file.size>10*1024*1024)throw Error(file.name+' is larger than 10MB.');const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=`${tenantId}/inventory/${asset.id}/${crypto.randomUUID()}.${ext}`;const h=new Headers({'Content-Type':file.type,'x-upsert':'false',apikey:key});if(session?.access_token)h.set('Authorization',`Bearer ${session.access_token}`);const rr=await fetch(`${SUPABASE_URL}/storage/v1/object/tradeflow-media/${path}`,{method:'POST',headers:h,body:file});if(!rr.ok)throw Error(await rr.text()||'Photograph upload failed.');const media=(await api('/rest/v1/media_assets',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({tenant_id:tenantId,storage_bucket:'tradeflow-media',storage_path:path,original_filename:file.name,mime_type:file.type,byte_size:file.size,asset_kind:'inventory_photo',retention_policy:'sold_90_days_after_sale',created_by:session?.user?.id||null})}))[0];await api('/rest/v1/inventory_asset_media',{method:'POST',body:JSON.stringify({tenant_id:tenantId,inventory_asset_id:asset.id,media_asset_id:media.id,sort_order:order++})})}await loadSourceInformation(asset);$('sales-photo-upload').value='';msg('Photographs added to the inventory item.','success')}catch(e){msg(e.message||String(e),'error')}finally{button.disabled=false}}

const next={draft:[['ready','Mark ready']],ready:[['published','Publish']],published:[['reserved','Reserve'],['sold','Mark sold'],['delisted','Delist']],reserved:[['published','Return to published'],['sold','Mark sold'],['delisted','Delist']],sold:[],delisted:[]};
function statusLabel(status){const labels={draft:'Draft',ready:'Ready',published:'Published',reserved:'Reserved',sold:'Sold',delisted:'Delisted'};return labels[String(status||'').toLowerCase()]||String(status||'Unknown').replaceAll('_',' ')}
function listingStatusClass(status){return ['published'].includes(status)?'published':['draft','ready'].includes(status)?status:['reserved'].includes(status)?'reserved':['sold','delisted'].includes(status)?status:'ready'}function listingActionClass(label){const l=label.toLowerCase();return l.includes('sold')?'danger':l.includes('delist')?'danger':l.includes('reserve')?'secondary':'primary'}function render(){if(!rows.length){$('listings').innerHTML='<div class="empty">No listings match this filter.</div>';return}$('listings').innerHTML=`<div class="listing-table"><div class="listing-row listing-header" style="border:0;background:transparent;border-left:0;padding:2px 12px;color:#68757f;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.05em"><span>Reference</span><span>Title</span><span>Status</span><span>Price</span><span>Channel</span><span>Action</span></div>${rows.map(r=>{const statusClass=listingStatusClass(r.status);const actions=(next[r.status]||[]).map(([to,label])=>`<button class="listing-action ${listingActionClass(label)}" type="button" data-transition="${r.id}" data-from="${r.status}" data-to="${to}">${esc(label)}</button>`).join('');const editButton=!['sold','delisted'].includes(String(r.status||'').toLowerCase())?`<button class="listing-action edit" type="button" data-edit="${r.id}">EDIT</button>`:'';return`<div class="listing-row ${statusClass}"><div class="listing-reference-cell"><span class="listing-cell-label">Reference</span><button class="listing-reference" type="button" data-view="${r.id}">${esc(r.listing_reference)}</button></div><div class="listing-title-cell"><span class="listing-cell-label">Title</span><strong>${esc(r.title||'Untitled listing')}</strong></div><div><span class="listing-cell-label">Status</span><span class="listing-status">${esc(statusLabel(r.status))}</span></div><div><span class="listing-cell-label">Price</span><strong>${money(r.asking_price,r.currency)}</strong></div><div><span class="listing-cell-label">Channel</span><span>${esc(channels.find(c=>c.id===r.channel_id)?.name||r.channel_id)}</span></div><div><span class="listing-cell-label">Action</span><div class="listing-actions"><button class="listing-action view" type="button" data-view="${r.id}">VIEW</button>${editButton}${actions}</div></div></div>`}).join('')}</div>`;document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>beginView(b.dataset.view));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>beginEdit(b.dataset.edit));document.querySelectorAll('[data-transition]').forEach(b=>b.onclick=()=>changeStatus(b.dataset.transition,b.dataset.from,b.dataset.to))}
async function transition(id,from,to){return api('/rest/v1/rpc/transition_workflow_entity',{method:'POST',body:JSON.stringify({p_tenant_id:tenantId,p_entity_type:'listing',p_entity_id:id,p_expected_from:from,p_to_status:to,p_notes:null,p_metadata:{source:'selling-dashboard'}})})}
async function changeStatus(id,from,to){try{msg(`Changing listing to ${to.replaceAll('_',' ')}…`);const listing=rows.find(x=>x.id===id);await transition(id,from,to);if(listing?.asset_id&&to==='published'){const assetsForSync=await api(`/rest/v1/inventory_assets?select=id,status&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(listing.asset_id)}&limit=1`)||[];const asset=assetsForSync[0];if(asset?.status==='ready_for_sale')await api('/rest/v1/inventory_assets?id=eq.'+encodeURIComponent(asset.id)+'&tenant_id=eq.'+encodeURIComponent(tenantId),{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({status:'listed',updated_at:new Date().toISOString()})})}if(listing?.asset_id&&to==='delisted'){const active=await api(`/rest/v1/listings?select=id&tenant_id=eq.${encodeURIComponent(tenantId)}&asset_id=eq.${encodeURIComponent(listing.asset_id)}&status=in.(published,reserved)&limit=1`)||[];if(!active.length){const assetsForSync=await api(`/rest/v1/inventory_assets?select=id,status&tenant_id=eq.${encodeURIComponent(tenantId)}&id=eq.${encodeURIComponent(listing.asset_id)}&limit=1`)||[];const asset=assetsForSync[0];if(asset?.status==='listed')await api('/rest/v1/inventory_assets?id=eq.'+encodeURIComponent(asset.id)+'&tenant_id=eq.'+encodeURIComponent(tenantId),{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({status:'ready_for_sale',updated_at:new Date().toISOString()})})}}await load();show(id);msg('Listing status updated.','success')}catch(e){msg(e.message||String(e),'error')}}
async function carryInventoryMedia(assetId,listingId){const links=await api(`/rest/v1/inventory_asset_media?select=media_asset_id,sort_order&tenant_id=eq.${encodeURIComponent(tenantId)}&inventory_asset_id=eq.${encodeURIComponent(assetId)}&order=sort_order`)||[];for(const m of links)await api('/rest/v1/listing_media',{method:'POST',body:JSON.stringify({tenant_id:tenantId,listing_id:listingId,media_asset_id:m.media_asset_id,sort_order:m.sort_order||0})})}
function publicListingUrl(listingId){return 'public-site.html?tenant_id='+encodeURIComponent(tenantId)+'&page=product&listing='+encodeURIComponent(listingId||'')} 
function beginView(id){const r=rows.find(x=>x.id===id);if(!r)return msg('The selected listing could not be loaded.','error');const url=publicListingUrl(r.id);window.open(url,'_blank','noopener,noreferrer');}
function show(id){const r=rows.find(x=>x.id===id);if(!r)return;const channel=channels.find(c=>c.id===r.channel_id);const cat=categories.find(c=>c.id===r.category_id);const branch=branches.find(b=>b.id===r.branch_id);const edit=['sold','delisted'].includes(String(r.status||'').toLowerCase())?'':`<p><button class="listing-action edit" type="button" data-edit="${r.id}">EDIT LISTING</button></p>`;$('details').innerHTML=`<div class="small"><strong>${esc(r.listing_reference)}</strong> · ${esc(r.status)}</div><p><strong>${esc(r.title)}</strong><br>${esc(r.description||'No description')}</p><p>${money(r.asking_price,r.currency)} · Qty ${esc(r.quantity)} · Channel: ${esc(channel?.name||r.channel_id)} · Category: ${esc(cat?.name||r.category_id)}</p><p class="small">Inventory asset: ${esc(r.asset_id)} · Published: ${esc(r.published_at||'—')} · Sold: ${esc(r.sold_at||'—')}</p>${edit}`;$('details').querySelector('[data-edit]')?.addEventListener('click',e=>beginEdit(e.currentTarget.dataset.edit));}async function beginEdit(id,focused=false){
 const r=rows.find(x=>x.id===id);
 if(!r)return msg('The selected listing could not be loaded.','error');
 const asset=assets.find(x=>x.id===r.asset_id);
 if(!asset)return msg('The inventory asset for this listing could not be loaded.','error');
 editingListingId=r.id;
 const form=$('listing-form');
 if(form){
   form.scrollIntoView({behavior:focused?'auto':'smooth',block:'start'});
 }
 $('asset').value=asset.id;
 $('channel').value=r.channel_id||'';
 $('asset').disabled=true;
 $('channel').disabled=true;
 const submit=form?.querySelector('button[type="submit"]');
 if(submit){submit.textContent='UPDATE LISTING';submit.disabled=false;}
 if($('cancel-edit'))$('cancel-edit').hidden=false;
 $('title').value=r.title||asset.title||'';
 $('description').value=r.description||'';
 $('price').value=r.asking_price??'';
 $('currency').value=(r.currency||asset.currency||'GBP').toUpperCase();
 $('asset-serial').value=asset.serial_number||'';
 $('asset-quantity').value=Number(asset.quantity)||1;
 $('asset-location').value=asset.location||'';
 $('category-display').textContent=categories.find(x=>x.id===asset.category_id)?.name||'Inherited from original buying category';
 $('branch-display').textContent=branches.find(x=>x.id===asset.branch_id)?.name||'Inherited from original buying branch';
 const data=r.listing_data||{};
 const condition=String(data.condition||'').trim().toLowerCase().replaceAll(' ','_');
 $('asset-condition').value=['poor','good','very_good','excellent','opened','never_used','sealed'].includes(condition)?condition:'';
 const shipping=data.shipping||{};
 $('shipping-method').value=shipping.method||'customer_pays';
 $('shipping-price').value=shipping.price!=null?shipping.price:0;
 $('dispatch-time').value=shipping.dispatch_time||'';
 if($('condition-source-note'))$('condition-source-note').textContent='Editing the existing retail listing. Changes will update the live website listing.';
 await loadSourceInformation(asset);
 await loadListingPhotos(r.id);
 if(submit){submit.textContent='UPDATE LISTING';submit.disabled=false;}
 if($('cancel-edit'))$('cancel-edit').hidden=false;
 msg('Editing listing '+r.listing_reference+'.','success');
}
function cancelEdit(){
 editingListingId=null;
 $('asset').disabled=false;
 $('channel').disabled=false;
 const submit=$('listing-form')?.querySelector('button[type="submit"]');
 if(submit){submit.textContent='SEND TO WEBSITE';submit.disabled=false;}if($('cancel-edit')){$('cancel-edit').textContent='CANCEL EDIT';$('cancel-edit').hidden=true;} if($('cancel-edit'))$('cancel-edit').hidden=true;
 $('listing-form')?.reset();
 $('currency').value='GBP';
 const asset=$('asset').value;
 if(asset)syncAssetContext();
 msg('Listing edit cancelled.','success');
}
$('listing-form').onsubmit=async e=>{
 e.preventDefault();
 if(!tenantId)return;
 try{
   const asset=$('asset').value,channel=$('channel').value,title=$('title').value.trim(),price=Number($('price').value),currency=$('currency').value.trim().toUpperCase();
   const condition=$('asset-condition').value;
   const shippingMethod=$('shipping-method').value;
   const shippingPrice=Number($('shipping-price').value||0);
   const dispatchTime=$('dispatch-time').value.trim()||null;
   if(!condition)throw Error('Select the retail condition before saving this listing.');
   if(!Number.isFinite(shippingPrice)||shippingPrice<0)throw Error('Enter a valid postage price.');
   if(!title||!Number.isFinite(price)||price<0)throw Error('Complete the title and asking price.');
   const listingData={source:'selling-dashboard',condition,shipping:{method:shippingMethod,price:shippingPrice,dispatch_time:dispatchTime}};
   if(editingListingId){
     msg('Saving listing changes…');
     const updated=await api('/rest/v1/listings?id=eq.'+encodeURIComponent(editingListingId)+'&tenant_id=eq.'+encodeURIComponent(tenantId),{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify({title,description:$('description').value.trim()||null,asking_price:price,currency,listing_data:listingData})});
     const saved=(updated||[])[0];
     await load();
     if(saved){await beginEdit(saved.id);show(saved.id);}
     msg('Listing changes saved and published website details updated.','success');
     return;
   }
   msg('Creating listing…');
   const assetRow=assets.find(a=>a.id===asset);
   if(!assetRow)throw Error('Select a valid inventory asset.');
   if(assetRow.status==='listed')throw Error('This item is already listed on the website. Use EDIT on the existing listing instead.');
   const existing=(await api('/rest/v1/listings?select=id,listing_reference,status,title,asking_price,currency&tenant_id=eq.'+encodeURIComponent(tenantId)+'&asset_id=eq.'+encodeURIComponent(asset)+'&status=not.in.(sold,delisted)&limit=1'))||[];
   if(existing[0]){msg(`This item is already listed as ${existing[0].listing_reference}. Use EDIT to change it.`,'success');await load();return}
   const category=assetRow.category_id,branch=assetRow.branch_id;
   if(!asset||!channel||!category||!branch)throw Error('Select a valid inventory asset and sales channel.');
   const data=await api('/rest/v1/listings',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({tenant_id:tenantId,asset_id:asset,channel_id:channel,category_id:category,branch_id:branch,title,description:$('description').value.trim()||null,asking_price:price,currency,quantity:1,listing_data:listingData,created_by:session?.user?.id||null})});
   const created=(data||[])[0];
   if(created?.id){await carryInventoryMedia(asset,created.id);await transition(created.id,'draft','ready');await transition(created.id,'ready','published');await transition(assetRow.id,'ready_for_sale','listed')}
   e.target.reset();$('currency').value='GBP';await load();if(created?.id)show(created.id);msg('Listing published to the selected sales channel and inventory marked listed.','success');
 }catch(e){msg(e.message||String(e),'error')}
};
function applyFocusedProductMode(){const focusAsset=new URLSearchParams(location.search).get('asset');if(!focusAsset)return;document.body.classList.add('focused-product');const panel=$('listings-panel');if(panel){panel.hidden=true;panel.style.display='none';}const selector=$('asset');if(selector)selector.disabled=true;const a=assets.find(x=>x.id===focusAsset);if(a&&$('product-heading'))$('product-heading').textContent=a.title||'Product';}
$('upload-sales-photos').onclick=uploadSalesPhotos;$('cancel-edit').onclick=cancelEdit;$('status-filter').onchange=load;$('refresh').onclick=load;$('sign-out').onclick=()=>{localStorage.removeItem(SESSION_STORAGE);location.href='customer-dashboard.html';};load();
function syncAssetContext(){const a=assets.find(x=>x.id===$('asset').value);if(a){$('title').value=a.title||'';$('description').value=a.description||'';$('currency').value=(a.currency||'GBP').toUpperCase();$('price').value=a.current_value??'';$('asset-condition').value='';$('asset-serial').value=a.serial_number||'';$('asset-quantity').value=Number(a.quantity)||1;$('asset-location').value=a.location||'';$('category-display').textContent=categories.find(x=>x.id===a.category_id)?.name||'Inherited from original buying category';$('branch-display').textContent=branches.find(x=>x.id===a.branch_id)?.name||'Inherited from original buying branch';loadSourceInformation(a)}else{$('title').value='';$('description').value='';$('currency').value='GBP';$('price').value='';$('asset-condition').value='';$('asset-serial').value='';$('asset-quantity').value='';$('asset-location').value='';$('category-display').textContent='Inherited from original buying category';$('branch-display').textContent='Inherited from original buying branch';$('source-information').innerHTML='<div class="empty">Select an inventory asset to load the source information.</div>';$('sales-photos').innerHTML='<div class="empty">Select an inventory asset to load photographs.</div>';}}
